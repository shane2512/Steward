import { ClosureChecklist } from '@/components/settings/ClosureChecklist';

export const metadata = { title: 'Close account - Steward' };

export default function ClosureChecklistPage() {
  return <ClosureChecklist />;
}
