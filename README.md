# Steward

The self-driving treasury that can't run off with the money.

Agent-operated stablecoin treasury (Coinbase AgentKit + SERV Reasoning). *Reasoning proposes, code disposes.*

Status: Phase 0 (verification spike). Specs live in `docs/`; start with `CLAUDE.md`.
